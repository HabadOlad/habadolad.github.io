# frozen_string_literal: true

source "https://rubygems.org"

# gem "rails"
gem "github-pages", group: :jekyll_plugins
#bundle exec jekyll serve starts the server
# These lines fix the "failed to load command" errors on modern Ruby
# Required fixes for Ruby 3.4 / 4.0 compatibility
gem "webrick"
gem "csv"
gem "bigdecimal"
gem "base64"
gem "mutex_m"